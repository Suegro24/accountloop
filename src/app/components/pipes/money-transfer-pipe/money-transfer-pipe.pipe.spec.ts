import { MoneyTransferPipePipe } from './money-transfer-pipe.pipe';

describe('MoneyTransferPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MoneyTransferPipePipe();
    expect(pipe).toBeTruthy();
  });
});
