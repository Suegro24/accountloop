import { BudgetListPipePipe } from './budget-list-pipe.pipe';

describe('BudgetListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BudgetListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
